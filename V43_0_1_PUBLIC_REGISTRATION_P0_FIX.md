# V43.0.1 公网注册 P0 修复

- 修复 `/api/auth/register` 在 SMTP 不可用时返回 HTTP 500。
- Railway 邮件服务不可达时，注册流程自动降级为即时激活，不再阻断所有外部客户。
- 保留邮箱验证能力：SMTP 正常时仍发送验证码。
- 可通过 `EMAIL_VERIFICATION_FAIL_OPEN=false` 强制邮件发送失败时拒绝注册。
- 注册降级仍执行设备/IP 风险判断、免费额度控制、用户钱包初始化和会话创建。
- 邮件异常被记录为结构化日志，不再变成无说明的服务器崩溃。
