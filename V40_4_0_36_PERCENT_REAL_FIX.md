# V40.4.0 — 36% 卡点与运行版本一致性修复

- 只有本轮实际生成“上下双语换行”的单元格才执行多行版式修复。
- 缓存全命中、0 项改动、左右分列、同行显示、仅译文模式全部跳过 XLSX 全包重扫。
- 每次启动清空旧 backend.log，并在日志首行写入版本、工程根目录和 Python 路径，避免旧日志误导。
- 启动健康检查同时校验版本号和工程根目录，禁止同版本的其他目录进程被误复用。
- 启动前清理 Python __pycache__，避免复制工程后加载旧字节码。
