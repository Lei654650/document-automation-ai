# V19.2.1 输出目录与智能整理修复

## 输出目录
- 单个文件的“打开所在文件夹”会直接选中该交付文件。
- 下载弹窗改为“打开交付包所在位置”，直接选中 `delivery_packages` 中的同名 ZIP。
- 项目级打开操作不再进入内部 `job_xx` 实现目录。

## Excel 智能整理 V2
- 识别工作表真实数据范围。
- 清除真实数据区之外的尾部空样式单元格和纯空尾部行。
- 修复被放大到 XFD/百万行的工作表维度。
- 不移动任何有内容的单元格，不删除公式或数值。
- 保留样式、图片、超链接、合并单元格、列宽、行高、冻结窗格及打印设置。
- 对客户异常文件 `st04` 的验证中，工作表 XML 由约 407 MB 缩减到约 7 MB，全部 25,058 个有效单元格坐标与内容保持一致。
