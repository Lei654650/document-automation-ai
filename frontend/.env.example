# Local development backend
VITE_API_BASE=http://localhost:8000

# Vercel production uses frontend/.env.production by default.
# You can also set VITE_API_BASE in Vercel Project Settings if the backend URL changes.
