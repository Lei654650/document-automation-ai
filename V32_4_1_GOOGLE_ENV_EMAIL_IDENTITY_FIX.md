# V32.4.2 Google 环境变量与邮箱身份中心修复

- 修复复制配置时下划线被替换为破折号或空格，导致 Google Client ID 无法读取。
- 启动时自动兼容并修复常见错误键名。
- Google 配置状态接口返回明确状态，前端禁用缓存读取。
- 注册和登录统一使用“邮箱地址 + 密码”，注册保留确认密码。
- 邮箱输入框增加 `you@example.com`，支持所有合法标准邮箱。
- 优化身份认证页面尺寸、输入框、按钮和第三方登录区域间距。
- 生产页面不再暴露具体环境变量名称。
