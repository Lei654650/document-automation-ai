# V38.0.0 本地优先性能重构

- 将自动化/HMI 通用规则 `_automation_vi_fallback` 移到 AI 请求之前执行。
- 高置信度本地翻译、PLC 对象规则和历史记忆先完成，剩余复杂文本才进入 AI。
- 默认 API 并发由 5 提升为 6；短标签批次调整为 160 项/18000 字符，减少超大 JSON 截断后的递归重试。
- 默认请求超时由 90 秒降为 60 秒，默认重试由 2 次降为 1 次，避免单个失败批次长时间阻塞。
- 新增 AI 批量阶段统计日志：输入、记忆命中、术语命中、实际 AI 项数、耗时和剩余项数。
- 翻译缓存命名空间升级，避免继续复用旧版本污染结果。
