# V34.2.0 翻译性能重构

本版本仅处理性能与性能回退保护，不调整现有页面布局。

## 已修复

- 翻译记忆库批量读取不再为每个命中同步写入 `hit_count`，避免多文件任务被 SQLite 全局锁串行化。
- 文件级并发与翻译批次并发分层限流，默认最多 2 个翻译文件并行、全局最多 5 个供应商请求，避免 3×5 请求叠加导致限流、重试和长时间停留在 70%。
- XLSX 写回先生成临时包，再原子替换正式输出；Windows 杀毒、预览器短暂占用时自动退避重试，避免 WinError 32 使整个 Worker 失败。
- OOXML 写回压缩级别由 6 调整为 3，在保持标准 XLSX 格式的同时减少 CPU 压缩耗时。
- 每个 Excel 输出记录阶段性能：扫描、翻译记忆与 AI、OOXML 写回、版式/占位符修复、交付校验。

## 长期性能保护

- 新增性能回归测试，保护翻译记忆库只读路径和输出原子发布。
- 后续版本发布前必须保留 `PERF file=... stage=... elapsed=...` 日志。
- 后续功能修改不得恢复逐命中数据库 UPDATE，也不得让文件并发与 API 批次并发无上限相乘。
- 固定基准任务：20 个 Excel、约 35–40 个工作表、约 2,000 条唯一文本。总耗时或任一阶段较稳定版退化超过 15% 时，不允许发布。
