# V32.3.5 邮箱验证码发送服务重构

- 验证码邮件先真实发送成功，再写入数据库并启动限流。
- SMTP 发送失败时不保存验证码、不进入冷却时间。
- 限流只统计真实成功发送的邮件：默认 60 秒冷却，15 分钟最多 5 次。
- 增加 SMTP 连接、TLS、认证和投递阶段日志。
- 对授权码错误、连接超时、端口/SSL 错误、发件人或收件人被拒绝返回明确提示。
- 开发验证码仅在显式启用且非云端模式下使用。
- 未修改启动脚本、运行方式以及其他业务模块。
