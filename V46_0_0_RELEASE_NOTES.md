# Document Automation AI V46.0.0 发布说明

发布日期：2026-08-06

## 本次重点

### 1. 修复新客户无法登录

- 后端区分邮箱未验证、账户停用、账户暂停和其他不可用状态。
- 未验证邮箱登录时返回结构化错误码 `EMAIL_NOT_VERIFIED`。
- 前端自动进入邮箱验证码页面，并允许重新发送验证码。
- “请联系管理员”只用于确实被管理员停用或暂停的账户。
- 补充正式域名 `docai365.com` 与 `www.docai365.com` 的 CORS 支持。

### 2. 修复后端启动语法损坏

- 清除 `backend/app/main.py` 首行多余字符。
- 前后端版本统一为 `46.0.0`。

### 3. 重构支付系统

- 从正式运行代码、界面、安全策略和部署配置中移除 PayPal 与 Paddle。
- 统一使用 Stripe Checkout。
- 新增银行卡、支付宝和微信支付选择。
- 银行卡套餐支持 Stripe 自动续费订阅。
- 支付宝与微信支付套餐按一个月或一年单期预付，期满手动续购。
- 新增支付方式、计费模型数据库字段及兼容迁移。
- 支付状态接口只允许当前登录客户查询自己的订单。
- Demo 支付确认改为必须登录，避免通过邮箱参数确认他人订单。

### 4. Webhook 与入账安全

- Stripe Webhook 使用原始请求体进行签名验证。
- 支持同步完成、异步成功、异步失败、过期、续费成功、续费失败和订阅取消事件。
- 银行卡订阅每次成功续费后刷新当期套餐积分和有效期。
- 套餐、积分、钱包和流水入账采用幂等处理，重复事件不会重复发放。

### 5. 工程与部署清理

- 删除旧 PayPal 配置文档和未使用的重复后端入口。
- 删除工程内真实 `.env` 文件，保留无密钥示例文件。
- 更新 Railway、Render、Docker Compose 和 Windows 启动脚本。
- 更新前端生产构建并同步到后端静态目录。

## 兼容说明

- 原有账户、订单和钱包数据库通过启动迁移兼容，不要求手动修改数据库。
- 历史文档中的 PayPal/Paddle 版本记录仅作为版本历史保留，不参与运行。
- 支付宝和微信支付的真实可用性由 Stripe 账户地区、币种和 Dashboard 审核决定。
