# V32.3.2 Password Recovery Production Closure

## 本轮修复

- 正式环境绝不显示本地开发验证码；仅 localhost/127.0.0.1/::1 的 Vite 开发模式可见。
- 重置密码按钮仅在邮箱、验证码、强密码与二次确认全部有效时启用。
- 重置阶段邮箱设为只读，避免验证码与邮箱错配。
- 密码强度区域增加缺失条件的实时改进建议。
- 增加“修改密码后退出所有设备”的明确安全提示。
- 密码修改成功后显示成功动画、3 秒倒计时，并提供立即返回登录入口。
- 工程版本统一更新为 32.3.2。

## 安全说明

线上部署必须配置 SMTP 邮件服务。开发验证码只用于本机开发验收，不会在正式构建中显示。
