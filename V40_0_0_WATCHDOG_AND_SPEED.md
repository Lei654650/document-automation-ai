# V40.0.0 防卡死与速度修复

- AI 单请求默认硬超时调整为 35 秒，可通过环境变量覆盖。
- 默认网络重试改为 0，最多允许 1 次，避免接口异常导致成倍等待。
- 修复旧版实际请求次数比配置多 1 次的问题。
- AI 批次增加 80 秒 watchdog，超时批次取消并继续后续交付流程。
- 大批次异常恢复最多拆分 2 层，不再递归到逐条请求。
- 批次失败后交给本地 PLC/HMI 规则和质量复核，不再拖死整个订单。
- 实时日志新增失败批次、超时批次和剩余项目统计。
- 保留 V39 本地模板优先、缓存复用与受控并发能力。
